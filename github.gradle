apply plugin: 'maven-publish'

group 'no.fintlabs'

publishing {
    repositories {
        maven {
            name = 'GitHub'
            url = uri("https://maven.pkg.github.com/FINTLabs/${rootProject.name}")
            credentials {
                username = System.getenv("USERNAME")
                password = System.getenv("TOKEN")
            }
        }
    }
    publications {
        maven(MavenPublication) {
            from(components.java)
        }
    }
}
